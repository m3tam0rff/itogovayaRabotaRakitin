<Window x:Class="TradeApp.OrderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TradeApp"
        mc:Ignorable="d"
        Title="Ваш заказ / Корзина" Height="500" Width="550"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{StaticResource MainBackgroundBrush}">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="TxtClientInfo" Text="Заказчик: Гость" FontSize="14" FontWeight="Bold" Foreground="#B8860B" Margin="0,0,0,15"/>

        <ListView Grid.Row="1" x:Name="LvOrderItems" HorizontalContentAlignment="Stretch">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4" Padding="12" Margin="0,0,0,6" Background="White">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="{Binding Product.Name}" FontSize="14" FontWeight="Bold"/>
                                <TextBlock Text="{Binding Product.PriceWithDiscount, StringFormat='{}{0:N2} руб.'}" FontSize="12" Foreground="#666666"/>
                            </StackPanel>

                            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Quantity, StringFormat='{}{0} шт.'}" VerticalAlignment="Center" Margin="0,0,15,0" FontWeight="SemiBold"/>
                                <Button Content="Удалить" Padding="8,3" Background="#666666" Click="BtnRemoveItem_Click" Tag="{Binding Product.Id}"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <StackPanel Grid.Row="2" Margin="0,15,0,0">
            <Separator Margin="0,0,0,10"/>
            <Grid Margin="0,0,0,15">
                <TextBlock x:Name="TxtTotalSum" Text="Итого: 0.00 руб." FontSize="16" FontWeight="Bold"/>
                <TextBlock x:Name="TxtPickupCode" Text="Код: ---" FontSize="14" HorizontalAlignment="Right" Foreground="#B8860B" FontWeight="SemiBold"/>
            </Grid>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Content="Оформить заказ" Margin="0,0,5,0" Click="BtnSubmitOrder_Click"/>
                <Button Grid.Column="1" Content="Назад к покупкам" Margin="5,0,0,0" Background="#666666" Click="BtnClose_Click"/>
            </Grid>
        </StackPanel>
    </Grid>
</Window>
